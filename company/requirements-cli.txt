# Super-Agents Beautiful CLI Requirements
# Required dependencies for the interactive init command

# Core CLI framework (required)
click>=8.0.0

# Beautiful terminal UI
rich>=13.0.0

# Interactive menu selection (optional but recommended)
questionary>=1.10.0

# Table formatting for other commands
tabulate>=0.9.0
